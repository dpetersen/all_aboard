I am displaying a slide with id {{id}}!

{{#link-to "new_perspective_assignment" this}}Add stuff{{/link-to}}

{{outlet}}

<div class="slide">
  {{#if perspectiveAssignments}}
    <div class="gridster" {{bind-attr class="assignmentsSaving"}}>
      <ul>
        {{#each perspectiveAssignments}}
          {{view AllAboard.PerspectiveAssignmentView assignmentBinding="this"}}
        {{/each}}
      </ul>
    </div>
  {{else}}
    No perspectives.
  {{/if}}
</div>
